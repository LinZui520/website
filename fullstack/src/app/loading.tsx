'use client'

const Loading = () => {

  return (
    <div className={"h-screen w-full flex justify-center items-center"}>
      <span className={"select-none font-bold text-[#1d1d1f] text-[18px] md:text-[22px] lg:text-[26px]"}>
        Loading
      </span>
    </div>
  );
}

export default Loading;
